{{ form_start(form) }}
    {{ form_widget(form) }}
    <p class="mt-3 fw-light">El campo marcado con * debe ser completado.</p>
    <hr>
    <button class="btn btn-sm float-end btn-outline-primary">Guardar</button>
{{ form_end(form) }}
