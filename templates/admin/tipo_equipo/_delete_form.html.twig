<form method="post" action="{{ path('tipo_equipo_delete', {'id': tipo_equipo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tipo_equipo.id) }}">
    <button class="btn">Delete</button>
</form>
