{{ form_start(form) }}
{{ form_errors(form) }}
    {{ form_row(form.solicitanteTexto) }}
    {{ form_row(form.ubicacionPedido) }}
    {{ form_row(form.ubicacionTexto) }}
    {{ form_row(form.solicitud) }}
    {{ form_row(form.observacion) }}
    {{ form_row(form.prioridad) }}

    <p class="mt-3 fw-light">Los campos marcados con * deben ser completados.</p>
    <hr>

    <button class="btn btn-sm float-end btn-outline-primary">Guardar</button>

    {{ form_end(form, {render_rest: false}) }}
