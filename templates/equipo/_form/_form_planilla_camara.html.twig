{% import _self as _selfMacro %}
{{ form_start(form) }}
{{ include('equipo/_form/_form_planilla.html.twig', { 'form': form }) }}

{# Campos de acceso remoto #}
	{{ include('equipo/_form/_form_acceso.html.twig', {'form': form}) }}

{# Campos de almacenamiento #}
{{ include('equipo/_form/_form_almacenamiento.html.twig', {'form': form}) }}

{# Campos específicos de la cámara #}
<div class="col mb-1">
	{{ form_label(form.megapixeles) }}
	{{ form_widget(form.megapixeles) }}
</div>

<div class="col mb-1">
	{{ form_label(form.ups) }}
	{{ form_widget(form.ups) }}
</div>
<div class="col mb-1">
	{{ form_label(form.red) }}
	{{ form_widget(form.red) }}
</div>

<div class="col mb-1">
	{{ form_label(form.observacion) }}
	{{ form_widget(form.observacion) }}
</div>

<p class="mt-3 fw-light">Los campos marcados con * deben ser completados.</p>
<hr>
<button class="btn btn-sm float-end btn-outline-primary">Guardar</button>
{{ form_end(form) }}

{% block javascripts %}
	{{ encore_entry_script_tags('componente_app') }}
	{% for file in encore_entry_js_files('componente_app') %}
		<script type="text/javascript" src="{{ asset(file) }}" async="false"></script>
	{% endfor %}
{% endblock %}
