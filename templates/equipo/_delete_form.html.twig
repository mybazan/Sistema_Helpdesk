<form method="post" action="{{ path('delete_planilla_equipo', {'id': equipo.planillaEquipo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ equipo.planillaEquipo.id) }}">
    <button class="btn">Delete</button>
</form>

